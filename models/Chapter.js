export default class Chapter {
    constructor(id, nome, descricao, status, usuario_id) {
      this.chapter_id = id;
      this.chapter_nome = nome;
      this.chapter_descricao = descricao;
      this.chapter_status = status;
      this.usuario_id = usuario_id;
    }
  }